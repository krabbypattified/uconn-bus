{
  "name": "uconn-bus",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "hammerjs": "^2.0.8",
    "mapbox-gl": "^0.41.0",
    "prop-types": "^15.6.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "NODE_PATH=src react-scripts start",
    "build": "NODE_PATH=src react-scripts build && rimraf docs && cp -R build docs && echo 'uconn-bus.gaberogan.me' > docs/CNAME",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "rimraf": "^2.6.2"
  }
}
